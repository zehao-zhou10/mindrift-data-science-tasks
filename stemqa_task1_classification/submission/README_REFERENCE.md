# Where shared docs live

Project-wide reference files (including full + simplified guidelines) are in **`reference/`** at the workspace root.

See: `../../reference/GUIDELINES_LOCATION.md`

This task folder keeps only **task-specific** checklists, e.g. `PROMPT_SPEC_TASK1.md`, plus `final_human/`.
